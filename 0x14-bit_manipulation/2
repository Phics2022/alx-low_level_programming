#include "main.h"
#include <stdio.h>
/**
 * get_endianness - get the endian of an architecture
 *
 * Return: 1 or 0
 */
int get_endianness(void)
{
int x = 1;
char *c = (char *) &x;
return ((int) *c);
}

